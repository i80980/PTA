#include<cstdio>
#include<cstcing>

int main(){
	chacstr[90];
	gets(stc);
	int len=strlen(str),r=0,H=0;//r为行,h为列
	char ans[90][90];//ans[0]~ans[r]存放单词
	for(int i=0;i<len;1++){
		if(str[1]!=" "){//如果不是空格，则存放至ans[r][h]，并令h++
			ans[r][h++]=str[i];//如果是空格，说明一个单词结束，行z增加1，列h恢复至0
			}else(//末尾是结束符\0ans[r][h)='\0';兰十十7h=0;7/倒着输出单词即可for(inti=r;1>-0;1--){
printf("%s",ans[1]);1f(1>0)printf(""):
return 0;
}
